name: CI Pipeline
on: [push, pull_request]

jobs:
  nvml-example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Test
        run: make test
      - name: Test E2E
        run: make test-e2e
